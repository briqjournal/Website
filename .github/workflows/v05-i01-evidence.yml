name: v05-i01 evidence capture

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v05-i01 evidence is already captured under tmp/v05-i01-evidence; automatic capture is retired."
